{
  "name": "conduit-example",
  "version": "1.0.0",
  "private": true,
  "description": "Example workspace showing Conduit DI compiler usage",
  "scripts": {
    "build": "conduit generate-types",
    "build:dry": "conduit compile --dry-run",
    "list": "conduit list",
    "analyze:user": "conduit analyze userService",
    "analyze:notification": "conduit analyze notificationService",
    "test": "tsx test-workspace.ts",
    "clean": "rimraf src/generated"
  },
  "dependencies": {
    "conduit": "file:../../packages/conduit"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsx": "^4.19.1",
    "typescript": "^5.0.0"
  }
}
